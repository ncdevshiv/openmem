# OpenMem — Agent-Agnostic Autonomous Memory System

# Core Vector DB
lancedb>=0.12.0

# Vector embeddings
sentence-transformers>=2.2.0
torch>=2.0.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0

# Utilities
python-dateutil>=2.8.0
tqdm>=4.65.0

# LLM integration (optional — enables real AI features)
# litellm>=1.0.0        # Agent-agnostic LLM calls (OpenAI, Anthropic, Ollama, etc.)

# Optional: richer embeddings
# transformers>=4.30.0
# bitsandbytes>=0.41.0

# Optional: Honcho integration
# honcho-ai>=1.0.0
