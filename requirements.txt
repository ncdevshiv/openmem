# OpenMem — core dependencies
# (heavy/optional dependencies live in pyproject.toml extras: ml, mcp, llm, honcho)

lancedb>=0.12.0
pyarrow>=16.0.0
numpy>=1.24.0
python-dateutil>=2.8.0
tqdm>=4.65.0

# Optional — embeddings & reranker:   pip install -e ".[ml]"
# torch>=2.0.0
# sentence-transformers>=2.2.0
# transformers>=4.30.0
# pandas>=2.0.0

# Optional — MCP server:              pip install -e ".[mcp]"
# mcp>=2.0.0

# Optional — LLM-backed reflection:   pip install -e ".[llm]"
# litellm>=1.0.0

# Optional — Honcho integration:      pip install -e ".[honcho]"
# honcho-ai>=1.0.0
