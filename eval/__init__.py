"""
OpenMem evaluation package.

Hermetic retrieval-quality evaluation: deterministic golden corpus
(golden_corpus.py), versioned golden queries (golden_queries.json), and a
runner (run_eval.py) that scores VectorDB.search() with the pure metrics
from memory_store.retrieval_metrics.
"""
